# Test bootstrap
# Install python ansible dependency
echo "Testing install.sh script output for sometests."
# if command -v yum list | grep libselinux-python >/dev/null 2>&1; then
#     echo "Python dependency installed."
# else
#     sudo yum -y install libselinux-python
# fi
